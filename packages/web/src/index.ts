import { init, SDK_VERSION, SDK_NAME, log } from 'frontend-monitor-browser';
import { MonitorVue } from 'frontend-monitor-vue';
import { errorBoundaryReport } from 'frontend-monitor-react';
export { init, SDK_VERSION, SDK_NAME, MonitorVue, log, errorBoundaryReport };
